## 🚁 DRONE-WEB (Frontend Portal)

ส่วนนี้คือส่วน **Frontend** สำหรับการจัดการและตรวจสอบข้อมูลโดรน ถูกพัฒนาด้วย Next.js โดยทำหน้าที่เป็นส่วนติดต่อผู้ใช้ (User Interface) เพื่อ :

* ดู Config : แสดงรายละเอียดการตั้งค่าของโดรน (Drone ID, Drone Name, Light, Country)
* Submit Log : บันทึกค่าอุณหภูมิของโดรน
* Review History : แสดงตารางบันทึก Log อุณหภูมิย้อนหลัง

## ⚙️ เทคโนโลยีที่ใช้
* Next.js (App Router) : Framework หลักสำหรับการสร้างเว็บแอปพลิเคชัน React
* React : Library หลักสำหรับการสร้าง UI
* Tailwind CSS : สำหรับการจัดรูปแบบและการออกแบบ (อ้างอิงจาก `globals.css` และโครงสร้าง Next.js ทั่วไป)

## 🛠️ การตั้งค่าและการรัน

### 1. การติดตั้ง Dependencies

เปิด Terminal ในโฟลเดอร์โปรเจกต์และรันคำสั่ง:

```bash
npm install
```

### 2. ตั้งค่าตัวแปรสภาพแวดล้อม(.env)

```bash
NEXT_PUBLIC_API_BASE_URL = https://drone-server-2.onrender.com
NEXT_PUBLIC_DRONE_ID = 66010207
```

### 3. รันเซิร์ฟเวอร์

```bash
npm run dev
```
